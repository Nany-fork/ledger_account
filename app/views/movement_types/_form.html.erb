<%= form_with(model: movement_type) do |form| %>
  <% if movement_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movement_type.errors.count, "error") %> prohibited this movement_type from being saved:</h2>

      <ul>
        <% movement_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :movement_category_id, style: "display: block" %>
    <%= form.text_field :movement_category_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
